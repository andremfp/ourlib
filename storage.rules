rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Thumbnails are stored by ISBN.
    // Allow anyone to read them (public access).
    // Only allow authenticated users to create/update them.
    match /thumbnails/{isbn} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
